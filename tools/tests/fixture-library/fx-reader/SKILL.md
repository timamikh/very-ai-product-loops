---
node_type: card
kind: method
name: fx-reader
steps: [1]
prerequisites: []
reads: [register:hypotheses]
writes: [section:cjm]
evidence_standard: decision
volume_rule: "10–15 candidate stages before the cut"
selection_rule: "keep the stages a segment names"
rejects_shown: required
status: draft
version: 0.1.0
updated: 2026-09-02
---
# fx-reader

Reads `{#problems}` for the pain ranking and works from it — a genuine read with no atom (C1 fires).

## How to do it

1. Score each stage against the pains listed in `{#problems}`.
2. The price anchor is `{#competitor-pricing}`, which is filled later in this step; the first pass takes an assumption.
3. Run this after `{#segments}` exist; re-run with `{#value-defensibility}` recorded as a `<!--w:adds-->` supplement.

## Output

The map feeds `{#jtbd}` and → `{#solution}`; conclusions go into `{#idea}` via the primary method.

## Worklog & projection

Short.
