<!--
  template-fragment: step-close → writes into places that already exist. It adds no section of its
  own: a finding that belongs to a section goes through that section's worklog and an ordinary
  projection, so the section's shape stays its method's. What this fragment fixes is the three
  shapes this pass alone produces — the whole-step conclusions block, the open item, the entry.
-->

# 1 · The conclusions block — whole-step scope

Appended to the worklog of the section the finding belongs to, under the same heading
`projection` step 0 uses (one block per worklog, whichever pass wrote it), before projecting:

```markdown
## Orchestrator's conclusions
<!-- appended at step close, before re-projecting; ⚙️/[assumption] only -->

- The anxiety force here and the feasibility bet in `#solution` are one bet read from two sides —
  proving it collapses the strongest force against adoption. ⚙️ → landed in this section
- Tier-2 was the counter-signal to this ranking and never left this worklog. Now readable next to
  the moat: it may be the sharper proving ground even if tier 1 stays the scale bet. ⚙️ → `#to-clarify`
- The scores in the table rest on no evidence from the segment at all. [assumption] → `R-002`
```

- **Every line carries ⚙️ or `[assumption]`** — this pass reasons over the whole; it never sources.
- **Every line ends in where it went** — `landed in this section` · a `#section` · a register id ·
  `killed (why)`. A conclusion with no destination is the thing this pass exists to stop: a finding
  stranded in a worklog for the second time.
- **Whose worklog** — the section the finding is *about*. A finding about the step as a whole has no
  worklog and goes straight to shape 2.

# 2 · The open item — two kinds, said apart

In `#to-clarify`, each item naming which kind it is:

```markdown
- **The human chooses.** Lead segment stays tier 1 (⚙️, all three ranking grounds) vs tier 2 as the
  proving ground — `#segments`. Resolved at Step 3 (`where-to-play-how-to-win`), not here.
- **Nobody knows yet.** Every desirability claim in this step rests on the founder's read, with no
  evidence from the segment. Discovery interviews are the first thing the tactical loop should buy.
- **A later step owns it.** "At scale" in `H-001` has no threshold — Step 4 (`hypothesis-thresholds`).
```

The kinds are not decoration: *the human chooses* waits for a person, *nobody knows yet* waits for
work, and a step that files the second kind as the first reads as decided when it is merely unasked.

# 3 · The change-log entry

In the artifact's own change log — including the honest empty answer:

```markdown
### 2026-08-20 — step read whole (step close)
- **From → To:** six sections closed one at a time → 2 re-projected (`#problems` gained the judgement
  its rows implied; `#jtbd` links its anxiety force to `H-001`), 3 open items filed, `R-002` seeded,
  4 card headlines marked
- **Why:** the sections were written in sequence, each before the later ones existed — the links
  between them had no pass in which they could be found
- **Trigger:** step 1's own sections all worked, before `theses`
```

```markdown
### 2026-08-20 — step read whole (step close) — nothing the parts did not already say
- **From → To:** no change to any section; the four questions asked and answered in the negative
- **Why:** the reading is owed whether or not it finds anything; the empty answer is the record
- **Trigger:** step 1's own sections all worked, before `theses`
```
