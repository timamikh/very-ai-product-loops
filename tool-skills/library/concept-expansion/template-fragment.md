<!--
  template-fragment: concept-expansion → fills {#solution}
  Follow process/CONVENTIONS.md. ⚙️ = agent proposal awaiting approval.
-->

## Solution {#solution}

_How the product solves each problem above. No orphan features._

| Problem | How the product solves it | Confidence |
|---------|---------------------------|------------|
| <tier-1 pain, ranking order> | <the mechanism, not the feature label> | [sourced: … | assumption] |

**Closure:** <one token per row above, in row order — `closes` · `partly` · `conditional` · `none`> —
_how far each mechanism actually closes its pain; the judgement the mapping exists to make._

**What the mapping shows:** ⚙️ _the one line the table adds up to — how far the top pains are
actually closed, and what the concept leans on instead._ [assumption] <!-- card -->

**Feasibility bets:** <mechanisms not built before> → `H-…` (`type: feasibility`)

**Rejected (orphan features):**

| Capability | Why rejected |
|------------|--------------|
| … | no ranked pain / tier-3 only / duplicate mechanism / no backing asset |

_`no backing asset` is the reverse defect — a ranked pain, a verb, and nothing in the inventory that
produces it; it is also a feasibility bet above._

**Decided:** <!--d:date--> <YYYY-MM-DD> · **by:** <!--d:by--> <who — prefix ⚙️ while unconfirmed> ·
**alternatives considered:** <!--d:alts--> <one alternative weighed and why it lost, or what makes
the choice forced — a bare "none" is a defect>
