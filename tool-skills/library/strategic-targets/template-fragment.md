<!--
  template-fragment: strategic-targets → fills {#strategic-targets}
  Follow process/CONVENTIONS.md. ⚙️ = agent proposal awaiting approval.
-->

## Strategic targets {#strategic-targets}

_The horizon commitments: the nodes the strategy promises to move and the value each must reach by
the horizon date — read off the projection, not wished. Step-5 period targets ladder up to these.
`metric-tree` defines the node, this section commits its horizon value, `goal-targets` (Step 5)
sets the period value — three values, three owners._

**Horizon:** <date> — from `3#winning-aspiration` (never minted here).

<!-- card -->
**Commitment:** <one sentence — the North Star value promised at the horizon date, the scenario it is
read off, and the driver that must move most to get there>.

| Node (`M-…`) | Target at horizon | From scenario | Why this node | Confidence |
|--------------|-------------------|---------------|---------------|------------|
| M-… (North Star) | … | base / conservative / optimistic (`#financial-model`) | the winning logic leans on it: … | [assumption] |
| M-… | … | … | … | [assumption] |

_3–5 nodes, no more — when everything is promised, no miss is visible. A target no scenario
produces is a wish: fix the model or lower the target._

**Considered, left untargeted** — nodes deliberately not steered at the horizon (≠ forgotten).

| Node (`M-…`) | Why untargeted |
|--------------|----------------|
| … | follows mechanically from a targeted node · not on the winning logic's path · no honest read at this horizon |

**Decided:** <!--d:date--> <YYYY-MM-DD> · **by:** <!--d:by--> <who — prefix ⚙️ while unconfirmed> ·
**alternatives considered:** <!--d:alts--> <the scenario values not chosen, and why these — never
a bare "none">
