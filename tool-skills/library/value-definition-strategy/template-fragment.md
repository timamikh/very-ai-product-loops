<!--
  template-fragment: value-definition-strategy → re-projects {#value-defensibility}
  (the Step-1 section, revisited at Step 3: derivatives + trajectory join the base moats).
  Re-projection drops the section's confirmed: marker — it awaits re-confirmation.
  Follow process/CONVENTIONS.md for confidence tags, sources, and hypothesis IDs.
  ⚙️ marks agent-proposed defaults awaiting human approval.
-->

## Value & Defensibility {#value-defensibility}

**Core value (post-AI):** _what is valuable that a rebuilt-with-an-LLM clone would not have._

- …  [assumption]

**Base moats** _(re-tested in the chosen arena, against known competitors)_

| Moat | Layer | Have / Building / Aspiration | Survives an LLM rebuild? | Confidence |
|------|-------|------------------------------|--------------------------|------------|
| e.g. Unique data: … | hard | Have | Yes | [sourced: …] |
| e.g. Audience: … | soft | Building | Partly | [assumption] |

**Derivative moats** _(now derivable — customers/scale exist; each names its dependency)_

| Derivative | Depends on | Reachable when | Confidence |
|------------|------------|----------------|------------|
| e.g. Lock-in | exclusive integration X + a customer embedded in it | after integration X ships | [assumption] |

**Moat trajectory:** ⚙️ _which base moat converts into which derivative, over what horizon; what
erodes meanwhile; the order of construction the strategy leans on._

**Defensibility summary:** ⚙️ _lead moat = … ; durability = H/M/L ; why it holds = … ; leveraged in
`{#how-to-win}` and named per bet in `{#bets}`._

**Seeded hypotheses:** H-… (moat / derivative-dependency claim to validate) → hypothesis register.

**Candidates rejected as features, not moats** — what failed the post-AI rebuild test (including
derivatives with no dependency path), and why.

| Candidate value | Why it fails the rebuild test |
|-----------------|-------------------------------|
| … | reproducible by anyone with the same model and a week |
