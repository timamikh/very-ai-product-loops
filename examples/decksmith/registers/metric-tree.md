---
node_type: register
register: metric-tree
product: "Decksmith (fictional sample)"
updated: 2026-07-20
---

# Metric register — Decksmith

> **Definitions here; values only in `metrics.csv`** (dated rows, append-only — rule in
> `process/REGISTERS.md`). A changed definition mints a NEW id.
> **Born at Step 4 (Strategic Plan).** Empty at concept stage — the tree (North Star → drivers)
> is built once there is something to measure; before Step 4, `parent: — to clarify —`.
> At `concept-viability` the status keeps this light: name the ONE activation/value metric that
> proves the concept works, defer the rest.

| ID | Definition | Unit | Kind | Parent | Instrumentation | Target | Source |
|----|------------|------|------|--------|-----------------|--------|--------|
| — | _(none yet — the metric tree is born at Step 4)_ | — | — | — | — | — | — |

_Nodes that are `not-instrumented`/`proxy` are the instrumentation gap map — a first-class output of Step 4._
