# Instance config — HUMAN-authored decisions about this instance.
# Cycle position (current_step, gate ticks) lives in state.yaml, not here.
# Schema: process/reference/config-schema.md (linter check H).

product: "Decksmith (fictional sample)"
language: en
active_status: concept-viability
directions:
  - development
  - go-to-market
  - back-office
delegation: allowed

scope_note: >
  Fictional sample product used as the reference run of very-ai-product-loops. Decksmith is an
  AI slide generator whose bet is native, fully editable .pptx/.key output that also looks
  designed — the escape from the "pretty-but-locked vs editable-but-ugly" fork. In scope: the
  concept, its market, and a concept-viability strategy through a first sprint. Out of scope
  (per the founder brief): real-time collaboration, non-slide formats, and pricing (deferred to
  Step 3 Strategy). The product is fictional; the market it sits in is real, and every external
  claim is researched and dated.

sources:
  - sources/originals/founder-brief.md
