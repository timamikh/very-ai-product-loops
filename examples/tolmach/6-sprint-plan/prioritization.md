---
node_type: worklog
step: 6-sprint-plan
method: prioritization
produces: must
status: draft
updated: 2026-08-14
---

# Worklog — Back-office tasks + приоритезация (Толмач, спринт 1)

Метод: `prioritization` (must/backlog по вкладу в цель) + формат Task (Description/DoD/Why) для
back-office. Правило concept-viability: в must — только то, что даёт learning-сигнал; всё, что не
двигает `H-…`/`M-…`, — кандидат в cut.

## MUST — T-1 · Контур согласия на голос для теста — links: `M-consent-rate` · `R-007`/`R-009`
- **Description:** минимальный контур явного согласия на захват/хранение голосового профиля до
  энролмента (для F-1).
- **Definition of Done:** явное согласие показывается и фиксируется до энролмента; сырое аудио не
  хранится (только производный профиль); есть отзыв/удаление; тест-соответствие приватности пройдено.
- **Why:** нельзя снимать голос (дифференциатор M2) без согласия — приватность `R-007`, биометрия в
  интимных разговорах; блокер F-1.

## Приоритезация must (вклад в learning-цель `H-001`)
- **F-1** (dev) — must №1: сам тест `H-001`. **T-1** (back-office) — must (блокер F-1, без него нельзя
  снимать голос). **A-1** (gtm) — must (готовность рекрута к моменту прототипа). Всё три необходимы,
  чтобы к концу периода снять сигнал `H-001`/`H-021`. [assumption]

## BACKLOG (back-office, приоритезировано)
- **T-2 (rank 3)** · Измерить COGS/минуту инференса на прототипе — links `R-012`/`H-020`. DoD: замер
  стоимости real-time voice-clone на F-1.
- **T-3 (rank 5)** · Базовая аналитика/воронка активации — links `M-activation`. DoD: события
  онбординга→первый успешный разговор пишутся.

## Change log
### 2026-08-14 — created
- **From → To:** — → MUST T-1 (согласие на голос) + приоритезация must (F-1/T-1/A-1) + бэклог T-2/T-3
- **Why:** заполнить back-office `{#must}` и ранжировать бэклог по вкладу в `H-001`
- **Trigger:** шаг 6 (оркестратор)
